[package]
name = "bobcat-entry"
version = "0.1.0"
edition = "2024"

[lib]
name = "bobcat_entry"

[features]
alloc = []
std = ["alloc"]

[dependencies]
bobcat-maths = { path = "../bobcat-maths" }
const-hex = { workspace = true }
